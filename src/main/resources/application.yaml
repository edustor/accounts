spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/edustor_accounts
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
edustor:
  accounts:
    registration: false